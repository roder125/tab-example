import { OutlineButtonDirective } from './outline-button.directive';

describe('OutlineButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlineButtonDirective();
    expect(directive).toBeTruthy();
  });
});
